[build]
builder = "NIXPACKS"

[deploy]
startCommand = "PYTHONPATH=src alembic upgrade head && PYTHONPATH=src uvicorn vidyalaya_ai.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
